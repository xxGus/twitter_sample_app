<% if signed_in? %>
  <div class="row">
    <%= render 'shared/user_sidebar' %>
    <div class="span8">
      <h3>Micropost Feed</h3>
      <%= render 'shared/feed' %>
    </div>
  </div>  
<% else %>
  <%= render 'logged_out_hero_unit' %>
<% end %> 